
## 查看机器人实时位置命令

简洁版

```
ros2 topic echo /state_estimation --field pose.pose.position.x --field pose.pose.position.y --field pose.pose.orientation
```

```
ros2 topic echo /state_estimation --field pose.pose.position
```

## 快速记录一条路径的航点的位置命令

```
ros2 run legged_bringup record_waypoints.py
```

## 机械臂快速测试命令

```
source /opt/ros/humble/setup.bash
source /home/dog12/SCURC_Nav_Sim/install/setup.bash
ros2 run serial_driver serial_cmd_sender --ros-args -p port:=/dev/arm_port
```

### 吸取（Pick，控制位 0x01）

```
ros2 topic pub --once /arm_command std_msgs/msg/Float64MultiArray "{data: [0.0, -550.0, -100.0, 0.0, 1.0]}"
sleep 5
ros2 topic echo /arm_status --once
```

ACK 成功示例：`state=0x01`（Pick 完成），`result=0x00`（成功）

### 放置（Place，控制位 0x02）

```
ros2 topic pub --once /arm_command std_msgs/msg/Float64MultiArray "{data: [0.0, -500.0, -50.0, 0.0, 2.0]}"
sleep 5
ros2 topic echo /arm_status --once
```

- 坐标单位：**毫米**（arm_base 坐标系）
- `data[4]`：`1.0`=Pick / `2.0`=Place
- ACK 成功示例：`state=0x02`（Place 完成），`result=0x00`（成功）


### 先完成基础规划（若尚未做过）

```
ros2 launch legged_mission_planner_f_r base_plan.launch.py
```

### 在 UI 中标注箱子与归还区并导出，会生成 tmp/base_scenario.yaml。启动智力题规划 UI


3. ```
   ros2 launch legged_mission_planner_f_r quiz_plan.launch.py
   ```

### 在倒计时内发布话题（另开一个终端）

```
source /home/dog12/SCURC_Nav_Sim/install/setup.bash
```

示例：发布「仪器」种类（编号 2）

```
ros2 topic pub --once /mission/quiz_type std_msgs/msg/Int32 "{data: 2}"
```

# SCURC 导航走点规划

> 最后更新: 2026-06-29 | 坐标系: **map** (Nav2 `ComputePathToPose` → global costmap frame)

---

## 一、坐标系说明

Nav2 `ComputePathToPose` 将 goal 通过 `transformPosesToGlobalFrame` 变换到 **global costmap 的 frame = `map`**。

| 数据来源               | 坐标系 |           用作航点           |
| ---------------------- | ------ | :---------------------------: |
| `/aft_mapped_in_map` | map    |           ✅ 直接用           |
| `/state_estimation`  | odom   | 可近似使用（本系统差异 ~2cm） |

> **注意**: baselink 到 map x=0 有约 50cm 物理标定偏移，不影响航点坐标（航点是 map 绝对坐标，不是相对 baselink 的偏移）。echo 实测值直接写入 waypoints.yaml 即可。

---

## 二、区域划分

> 配置: `nav2_params.yaml` → `position_based_param_switcher` (lower=1.35, upper=4.0, hysteresis=0.1)

```
  边缘一区 (启动区)       中间区 (操作区)        边缘三区 (放置区)
←──────────────────┬─────────────────────┬──────────────────→
     x < 1.35        1.35 ≤ x ≤ 4.0         x > 4.0
```

**迟滞规则**（防止边界振荡）:

| 切换方向       | 判定条件                      |
| -------------- | ----------------------------- |
| 首次判定       | `1.35 ≤ x ≤ 4.0` → 中间区 |
| EDGE → MIDDLE | `x ≥ 1.45` 且 `x ≤ 3.9`   |
| MIDDLE → EDGE | `x ≤ 1.25` 或 `x ≥ 4.1`  |

**区域策略差异**:

|                    | 边缘区          | 中间区                        |
| ------------------ | --------------- | ----------------------------- |
| yaw                | 自由旋转        | 锁 0 (MaintainYawCritic=5000) |
| vy                 | ±1.4 m/s       | 0 (禁止横向)                  |
| RotateToGoal       | 32.0            | 0.0                           |
| GoalAlign          | 24.0            | 0.0                           |
| PathAlign          | 32.0            | 0.0                           |
| DecouplingCritic   | 30.0            | 0.0                           |
| yaw_goal_tolerance | 0.052 rad (3°) | 6.28 rad (360°)              |

---

## 三、箱子体系

### 种类

4 种，每种 2 个，共 8 个:

| 编号 | 颜色 |
| :--: | ---- |
|  0  | 绿色 |
|  1  | 灰色 |
|  2  | 蓝色 |
|  3  | 红色 |

> ⚠️ **8 个固定位置上具体放哪种箱子由裁判随机决定**，启动后通过人工标注或摄像头识别获知。

### 中间区布局

8 个固定点位，2 行 × 4 列。编号蛇形从右到左: **第一行右边 #0 → 第一行左边 #3 → 第二行右边 #4 → 第二行左边 #7**（两行都是从右往左编号）。

**第一行 (对应航点 3)** — 编号 #0 ~ #3:

```
    y →           右边
  ┌──────┬──────┬──────┬──────┐
  │  #3  │  #2  │  #1  │  #0  │
  └──────┴──────┴──────┴──────┘
```

**第二行 (对应航点 2)** — 编号 #4 ~ #7:

```
    y →           右边
  ┌──────┬──────┬──────┬──────┐
  │  #7  │  #6  │  #5  │  #4  │
  └──────┴──────┴──────┴──────┘
```

### 边缘区放置区

4 个放置区，编号 0/1/2/3（与箱子种类一一对应），每个可叠放 2 个同种箱子。

---

## 四、机器人行为状态

| 状态 | 含义 | 所在航点 | 说明                                                   |
| :--: | ---- | -------- | ------------------------------------------------------ |
|  0  | 调整 | 1, 4, 5  | 纯导航，根据上一出发点策略调整 yaw/y，为下一区域对齐   |
|  1  | 吸取 | 2, 3     | 机械臂吸取箱子: 发坐标 → 吸取 → 收完成帧 → 下一航点 |
|  2  | 放置 | 6        | 机械臂放置箱子: 发坐标 → 放置 → 收完成帧 → 下一航点 |

---

## 五、三条路径：箱子映射

每条路径覆盖中间区 4 个固定编号位置 + 边缘区 2 个放置区。

> ⚠️ 同一箱子编号在不同路径中，左右归属**一定不同**（机器人站位不同）。

|                          | 路径1 (最右)      | 路径2 (中间)      | 路径3 (最左)      |
| ------------------------ | ----------------- | ----------------- | ----------------- |
| **航点2** (第二行) | 左=#5 右=#4       | 左=#5 右=#6       | 左=#6 右=#7       |
| **航点3** (第一行) | 左=#1 右=#0       | 左=#1 右=#2       | 左=#2 右=#3       |
| **航点6** (放置区) | 左=放置区#1 右=#0 | 左=放置区#2 右=#1 | 左=放置区#3 右=#2 |

**跨路径左右变化示例**: 放置区#1 → 路径1 用左臂放置，路径2 用右臂放置。

---

## 六、路径选择策略（动态）

**原则**: 选择「该路径覆盖位置上的箱子种类」与「该路径放置区种类」匹配数最多的路径。

**流程**:

1. 启动时人工标注（或摄像头扫描）每个编号位置的箱子种类
2. 对每条路径统计匹配数，选出最优的一条路径
3. 直线来回吸放完该路径 4 个箱子
4. 在规划中去除已处理箱子，从剩余中二次选择，重复，直至选完三条路径，将所有箱子吸取完成。

**理想匹配分析**:

| 路径 | 放置区种类 | 覆盖的箱子编号 | 最大匹配数 |
| :--: | :--------: | -------------- | :--------: |
|  1  |   {0, 1}   | #5, #4, #1, #0 |    4 ★    |
|  3  |   {2, 3}   | #6, #7, #2, #3 |    4 ★    |

**跨路径放置**: 当某路径放置区已满，剩余同种箱子需在该路径吸取，搬运到另一路径的放置区。

---

## 七、一条路径直走走点详细（基准路径）

### 走点循环

```
起点 ──→ 航点1 ──→ 航点2/3 ──→ 航点4 ──→ 航点6 ──→ 航点5 ──→ (回到航点2/3)
 边缘策略   边缘策略   中间策略    中间→边缘   边缘策略   边缘→中间
 (对齐yaw)  (对齐yaw)  (yaw锁直走) (直走→自由) (自由)    (对齐后回中间区)
```

### 各航点坐标

| 航点            | 区域               | 状态 | 策略     | map 坐标 (x, y, z)                      |
| --------------- | ------------------ | :--: | -------- | --------------------------------------- |
| 起点            | 边缘一区 (x<1.35)  |  —  | —       | (0.035, 0.024, -0.041) ★`aft_mapped` |
| **航点1** | 边缘一区 (x=1.22)  | 0调整 | 边缘策略 | (1.223, 0.102, -0.005)`state_est`     |
| **航点2** | 中间区             | 1吸取 | 中间策略 | (2.158, -0.865, 0.116)`state_est`     |
| **航点3** | 中间区             | 1吸取 | 中间策略 | (2.970, -0.926, 0.012)`state_est`     |
| **航点4** | 边缘三区 (x=4.20)  | 0调整 | 中间策略 | (4.200, ≈-0.926, 待测)                 |
| **航点5** | 中间区 (x=3.80)    | 0调整 | 边缘策略 | (3.800, -0.9339, 待测)               |
| **航点6** | 边缘三区 (x=5.47)  | 2放置 | 边缘策略 | (5.468, -0.838, 0.073) ★`aft_mapped` |

> ★ = 推荐使用 `aft_mapped_in_map` 数据（map 帧）

### 各航点说明

**航点1** (过渡): 从启动区出发，全程边缘策略到达（x=1.223 < 1.35，仍在边缘区），yaw 和 y 已精确对齐。**之后**从航点1出发去航点2时，中途越过 x=1.45 才切换中间区策略，锁 yaw 直走。

**航点2** (吸取第二行): 左=#5, 右=#4。中间区策略，yaw 锁 0，纯 X 直走。

**航点3** (吸取第一行): 左=#1, 右=#0。同上，中间区策略直走。

**航点4** (中间→边缘过渡): 位于边缘区 (x=4.2 > 4.0)。从航点2/3 出发时使用中间策略直走，到 x=4.1 处切换为边缘策略，之后自由移动。y 在中间区段保持与航点2/3 一致（≈ -0.926）。

**航点5** (边缘→中间过渡): 从航点6 退回，用边缘策略重新对齐 yaw/y后，在 x=3.9 处切换为中间策略。x=3.8 在中间区内（1.35 ≤ 3.8 ≤ 4.0），留有迟滞余量。y = -0.9339。

**航点6** (放置): 边缘区策略自由移动。流程: 航点2/3（中间策略直走）→ x=4.1 处切换边缘策略 → 航点4（继续直走到 x=4.2）→ 自由移动到航点6。

---

## 八、路径2 和 路径3（相对路径1 的 y 偏移）

#### 偏移规则

|                 | 航点1~5                | 航点6                       |
| --------------- | ---------------------- | --------------------------- |
| **路径2** | 相对路径1: Δy = -85cm | 相对路径1航点4: Δy = -80cm |
| **路径3** | 相对路径2: Δy = -85cm | 相对路径2航点4: Δy = -80cm |

### 全量坐标

| 航点  | 路径1 (基准)  | 路径2      | 路径3      |
| ----- | ------------- | ---------- | ---------- |
| 起点  | y = 0.024 ★  | 同路径1    | 同路径1    |
| 航点1 | y = 0.102     | y = -0.748 | y = -1.598 |
| 航点2 | y = -0.865    | y = -1.715 | y = -2.565 |
| 航点3 | y = -0.926    | y = -1.776 | y = -2.626 |
| 航点4 | y = -0.926    | y = -1.776 | y = -2.626 |
| 航点5 | y = -0.926    | y = -1.776 | y = -2.626 |
| 航点6 | y = -0.838 ★ | y = -1.726 | y = -2.576 |

> x 坐标三路径相同。★ = map 帧实测，其余为推算值（待实测验证）。

---

## 九、跨路径走点策略

当某条路径中间区吸取的箱子种类，在本路径的放置区没有对应位置（或已满）时，需要跨到另一条路径的放置区进行放置，反之亦然。

### 9.1 两条基础路线

跨路径走点由两条基础路线组成，交替使用形成完整循环：

**路线 A: 跨路径吸取** — 从路径A 的放置区 → 路径B 的中间区吸取

```
PathA-WP6 ──────→ PathB-WP5 ──────→ PathB-WP2/3
  边缘策略          边缘策略          中间策略
  (yaw自由 vy自由)  (对齐yaw/y)      (yaw锁0 直走吸取)
```

| 段 | 起点 | 终点 | 策略 | y 变化 |
|----|------|------|------|--------|
| 1 | PathA-WP6 (边缘区) | PathB-WP5 (中间区 x=3.8) | 边缘策略 | PathA-y → PathB-y |
| 2 | PathB-WP5 (中间区) | PathB-WP2/3 | 中间策略 | 保持 PathB-y，直走 |

> 段1 在边缘区用边缘策略完成 y 方向平移 + yaw 对齐，到达 PathB-WP5 后切换中间策略。

**路线 B: 跨路径放置** — 从路径B 的中间区 → 路径A 的放置区放置

```
PathB-WP2/3 ──────→ PathB-WP4 ──────→ PathA-WP6
  中间策略          中间策略          边缘策略
  (yaw锁0 直走)     (yaw锁0 直走)    (yaw自由 vy自由)
```

| 段 | 起点 | 终点 | 策略 | y 变化 |
|----|------|------|------|--------|
| 1 | PathB-WP2/3 (中间区) | PathB-WP4 (边缘区 x=4.2) | 中间策略 | 保持 PathB-y，严格直走 |
| 2 | PathB-WP4 (边缘区) | PathA-WP6 (边缘区) | 边缘策略 | PathB-y → PathA-y |

> 段1 必须使用中间策略（yaw 锁、vy 禁），确保从吸取点直走到过渡点，y 不偏移。
> 段2 到达 PathB-WP4 后切换边缘策略，在边缘区自由移动到 PathA 的放置点。

### 9.2 完整跨路径循环

两条路线首尾相接，构成一个完整的跨路径吸放循环：

```
        吸取                             放置
  PathB-WP2/3 ──→ PathB-WP4 ──→ PathA-WP6 ──→ PathB-WP5 ──→ PathB-WP2/3
    (中间区)      (过渡)        (放置)        (过渡)         (回到吸取)

  策略: 中间 ─────────→ 边缘 ─────────→ 边缘 ──→ 中间 ──→ ...
              yaw锁直走    yaw自由平移   对齐yaw    yaw锁直走
```

关键设计意图：
- **WP4** 是中间区→边缘区的"出口"：从吸取点用中间策略直走到此，然后解锁 yaw/vy
- **WP5** 是边缘区→中间区的"入口"：在边缘区完成 y 平移和 yaw 对齐后进入，之后锁 yaw 直走
- 跨路径时，WP4 保持**本路径**的 y，WP5 也保持**本路径**的 y——y 切换发生在 WP4→WP6 和 WP6→WP5 的边缘区段

### 9.3 具体示例：路径2 ↔ 路径1

场景：路径2 中间区吸到了类型 0 的箱子，需放到路径1 的放置区 #0。

```
路径2 航点2/3          路径2 航点4            路径1 航点6
    │                      │                      │
    │  中间策略             │  边缘策略             │
    │  yaw锁 vy禁          │  yaw自由 vy自由       │
    │  y=-1.776            │  y: -1.776 → -0.838  │
    └──────────────────────┴──────────────────────┘
         x: 2.97 → 4.20          x: 4.20 → 5.47
         (本路径直走)             (跨路径y平移)
```

放置完成后，返回路径2 继续吸取：

```
路径1 航点6            路径2 航点5            路径2 航点2/3
    │                      │                      │
    │  边缘策略             │  中间策略             │
    │  yaw自由 vy自由       │  yaw锁 vy禁          │
    │  y: -0.838 → -1.776  │  y=-1.776            │
    └──────────────────────┴──────────────────────┘
         x: 5.47 → 3.8           x: 3.8 → 2.97
         (跨路径y平移)           (本路径直走)
```

### 9.4 所有跨路径组合

| 吸取路径 | 放置路径 | 吸取路线（去） | 放置路线（回） |
|:--------:|:--------:|---------------|---------------|
| P1→P2 | P1→P2 | P1-WP6 → P2-WP5 → P2-WP2/3 | P2-WP2/3 → P2-WP4 → P1-WP6 |
| P1→P3 | P1→P3 | P1-WP6 → P3-WP5 → P3-WP2/3 | P3-WP2/3 → P3-WP4 → P1-WP6 |
| P2→P1 | P2→P1 | P2-WP6 → P1-WP5 → P1-WP2/3 | P1-WP2/3 → P1-WP4 → P2-WP6 |
| P2→P3 | P2→P3 | P2-WP6 → P3-WP5 → P3-WP2/3 | P3-WP2/3 → P3-WP4 → P2-WP6 |
| P3→P1 | P3→P1 | P3-WP6 → P1-WP5 → P1-WP2/3 | P1-WP2/3 → P1-WP4 → P3-WP6 |
| P3→P2 | P3→P2 | P3-WP6 → P2-WP5 → P2-WP2/3 | P2-WP2/3 → P2-WP4 → P3-WP6 |

> 实际使用哪些组合取决于箱子摆放后的匹配结果。例如路径2 吸到类型0/1 的箱子 → 跨到路径1 放置；路径2 吸到类型3 → 跨到路径3 放置。

### 9.5 跨路径 y 偏移速查

跨路径移动时，y 方向需在边缘区段完成平移：

| 跨路径 | WP4→WP6 或 WP6→WP5 的 y 变化量 |
|:------:|:-------------------------------:|
| P1 ↔ P2 | Δy ≈ 89cm (P1-y 和 P2-y 的差) |
| P2 ↔ P3 | Δy ≈ 85cm |
| P1 ↔ P3 | Δy ≈ 174cm (P1-y 和 P3-y 的差) |

> 差值为近似值，以实测为准。边缘区 vy=±1.4m/s，平移耗时约 0.6~1.2 秒。

---

## 十、区域边界验证

当前配置: lower=1.35, upper=4.0, hysteresis=0.1

| 航点  | x     | 区域     |                     验证                      |
| ----- | ----- | -------- | :-------------------------------------------: |
| 起点  | ≈0   | 边缘一区 |                 ✅ x < 1.35                   |
| 航点1 | 1.223 | 边缘一区 |   ✅ 1.22 < 1.35，边缘策略对齐 yaw/y 后进入   |
| 航点2 | 2.158 | 中间区   |   ✅ 1.35 ≤ 2.16 ≤ 4.0，且 > 迟滞进入点 1.45 |
| 航点3 | 2.970 | 中间区   |   ✅ 1.35 ≤ 2.97 ≤ 4.0，且 < 迟滞退出点 3.9  |
| 航点4 | 4.200 | 边缘三区 |           ✅ 4.2 > 迟滞退出点 4.1            |
| 航点5 | 3.800 | 中间区   |   ✅ 1.35 ≤ 3.8 ≤ 4.0，留有迟滞余量          |
| 航点6 | 5.468 | 边缘三区 |                      ✅                       |

---

## 十一、待办

- [ ] 航点4: y/z 用 `aft_mapped_in_map` 实测
- [ ] 航点5: y/z 用 `aft_mapped_in_map` 实测确认
- [ ] 路径2/3: 所有航点实测确认（尤其航点6 y 偏移）
- [ ] waypoints.yaml: 为三条路径分别配置完整航点序列
- [ ] 路径选择算法: 根据实际箱子摆放动态计算最优路径
- [ ] 机械臂吸取/放置坐标: 每种箱子编号的左右臂具体坐标
- [ ] 智力题识别集成

---

## 附录：原始测量数据

```
原点启动:
  /state_estimation:     (0.005, -0.019, -0.002)
  /aft_mapped_in_map:    (0.035,  0.024, -0.041)

路径1 航点1:  /state_estimation  (1.223,  0.102, -0.005)
路径1 航点2:  /state_estimation  (2.158, -0.865,  0.116)
路径1 航点3:  /state_estimation  (2.970, -0.926,  0.012)
路径1 航点4:  /state_estimation  (4.200,  (待测), (待测))
路径1 航点5:  (全部待测)
路径1 航点6:  /aft_mapped_in_map  (5.468, -0.838,  0.073)
              /state_estimation   (5.446, -0.836,  0.028)
```

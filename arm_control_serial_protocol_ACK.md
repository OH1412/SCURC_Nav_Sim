
# 下位机 ACK 握手协议（机械臂状态回传）

## 概述

本协议用于机械臂下位机在执行完动作（Pick / Place）后，
通过串口向上位机发送执行结果，实现闭环控制与行为树同步。

---

## 1. ACK帧格式

ACK帧为固定 **6 字节**数据帧：

| 字节索引 | 字段名   | 大小 | 示例值      | 说明     |
| -------- | -------- | ---- | ----------- | -------- |
| 0        | 帧头1    | 1    | 0xFE        | 固定     |
| 1        | 帧头2    | 1    | 0xFE        | 固定     |
| 2        | 长度     | 1    | 0x03        | 固定     |
| 3        | 状态码   | 1    | 0x01 / 0x02 | 动作类型 |
| 4        | 结果码   | 1    | 0x00 / 0x01 | 执行结果 |
| 5        | checksum | 1    | 计算值      | 校验和   |

---

## 2. 状态码定义（State）

| 状态码 | 含义                  |
| ------ | --------------------- |
| 0x01   | Pick（吸取动作完成）  |
| 0x02   | Place（放置动作完成） |

---

## 3. 结果码定义（Result）

后续再拓展判断逻辑，现在只需要到达位置，完成基本动作就成功执行

| 结果码 | 含义     |
| ------ | -------- |
| 0x00   | 成功执行 |
| 0x01   | 执行失败 |

---

## 4. checksum 计算规则

采用简单累加取低8位：

```text
checksum = (0xFE + 0xFE + 0x03 + state + result) & 0xFF
```

## 5. ACK发送时机

下位机必须在**动作完全完成后**发送 ACK：

### Pick流程：

1. 机械臂到达目标位置
2. 执行吸取动作（吸附开启）
3. 动作稳定确认
4. 发送 ACK（state = 0x01）

---

### Place流程：

1. 到达目标位置
2. 执行放置动作（电磁阀释放，并吹气）
3. 确认物体释放完成
4. 发送 ACK（state = 0x02）

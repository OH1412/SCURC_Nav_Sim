# 前后吸取路径规划要求和限制

还是将等待智力题进行智力题规划和不需要智力题的基础规划分开，分成两个launch文件，和两种ui设计。

具体就是：

base_plan就是操作手手动点击箱子和归放区，然后点击规划后，输出基础规划，并保留点击的箱子和归放区的编号。然后quiz_plan就是加载基础规划的输出箱子和归还区编号后，进行基于收到的智力题的智力题规划。两个launch文件，智力题的规划ui 不需要任何点击，只需要一个显示倒计时的，倒计时可修改，暂定为15秒，时间内收到了0-3的智力题答案，就进行自动的智力题规划，覆盖基础规划的输出，将其作为最终规划，反之，将基础规划作为最终规划。

（已完成）

修改：

1.将路径规划还是分成两个阶段，基础路径规划和基于基础路径规划的智力题规划，ui也需要进行修改，基础路径规划的ui的默认箱子类型也应该要和归放区的默认类型进行对齐，改变放置区类型后，也要同步对齐默认箱子的类型。放置区是编号1，那么这一列的箱子也要是编号1。

2.智力题的规划还是要进行可以修改的计时操作，在时间内完成智力题的答案的接收就进行自动智力题规划，没有收到就不进行智力题规划。

（已实现的修改）

路径4 规划已固定，并排除 path4 的 box2/box6 对其它箱子路径规划的干扰：

- **执行顺序**：先 path4 wp2 吸取并放置 box2 → 主任务（box 0/1/3 再 4/5/7，最近路径策略不变）→ 最后 path4 wp1 吸取并放置 box6
- **五元组导出**：path4 的 box2/box6 **仅导出 PLACE**（p2wp3 / p3wp3 / p4wp3 / p5wp3 之一，由 box 种类映射决定）；PICK 由行为树硬编码
- **导出序列**：首步为 box2 放置点，末步为 box6 放置点（同 zone 第二次放置 `target_id = zone + 4`

（已实现的修改）

将智力规划和基础路径规划和并，重构路径规划。具体修改为，a.将智力题作为可选项，默认-1，表示没有计算出智力题，在下拉列表ui中选择好智力题计算出的编号0,1,2,3后，点击路径规划的按键ui后，直接输出智力题规划结果，如果没有选，那就是-1的基础路径规划结果。 b.改成只有一个launch文件，最后直接输出最终的路径规划，也就是最终yaml文件。不再需要原先的两种launch文件。

**（已回退）** 上述「下拉选智力题 + 单次导出」方案已回退，恢复为两阶段流程（见文档顶部修改 1、2 及下方「（一）两阶段路径规划 UI」）。仍保持单一 `mission_plan.launch.py` 入口。

(已实现的修改)

现在不需要路径1,将路径1完全删除，原本的路径1的1号点位就改成路径2的1号点位,路径2的1号点为改成2号点位，路径2的2号点位改成路径2的3号点位。之后就直留下2,3,4,5四条路径，每条路径就是各3个点位。（注意单功能包修改不要涉及其它功能包）

## 规划策略摘要（已实现）

**航点配置**：`config/ui_points_fast_mode.yaml`（标定导出，含每条路径的实际 wp 数、状态、target_id、motion_planner 与 UI 坐标）。规划器启动时自动加载。

**四阶段执行顺序（硬约束）**：

| 阶段        | 箱子              | 说明                                                 |
| ----------- | ----------------- | ---------------------------------------------------- |
| 1. path4 首 | **2 号**    | path4 wp2 吸取 box2 → 跨路径直达对应归还区 wp3 放置 |
| 2. 第一行   | **0、1、3** | box2 已单独处理；须全部完成后再进入阶段 4            |
| 3. 第二行   | **4、5、7** | 第一行 3 个全部 pick+place 后才开始                  |
| 4. path4 末 | **6 号**    | path4 wp1 吸取 box6 → 跨路径直达对应归还区 wp3 放置 |

**主任务组内排序**（第一行、第二行各自独立；不含 path4 的 box2/box6）：

1. 智力题种类（二次规划时，`quiz_type` 匹配优先，但不打破行序）
2. 路径距离 `abs(current_path - pick_path)`（**最近路径优先**；允许同种类箱子放置两次）
3. `box_id` tie-break

**移动规则**：

- path2–5 主任务：**不强制经过中间航点**；跨路径或同路径内 pick 后直达 place 航点（无过渡走点）
- path4 box2/box6：吸取后直达对应归还区放置，不经过过渡航点
- 航点 wp 索引与放置位置取自 `ui_points_fast_mode.yaml`（path2–5 放置均为 wp3）

**输出**（默认 `tmp/`）：

- `mission_plan.yaml` / `.json` / `.csv` — 逻辑七元组序列（含 path4 box2/box6 的 PICK/PLACE）
- `mission_quintuple.yaml` — 五元组 `(path, wp, state, target_id, motion_planner)` 供行为树；**path4 box2/box6 仅导出 PLACE**，首步 box2 放置、末步 box6 放置
- `mission_plan.png` — 按 ui_points 标定坐标划线

---

修改方向3：

让路径规划使用上路径和航点标定时导出的配置文件。已标定 `config/ui_points_fast_mode.yaml`，规划器启动后规划出的点位均使用该配置文件中的点位。

fast_mode 策略，保底阶段直接在 path4 wp1 吸取 6 号箱子（box6），并到对应归还区进行放置，此时可能处在 2、3、4、5 路径中的任意一条。

此时，已经有一个归还区有箱子了，剩下三种归还区还没有箱子，

接下来就是先将第一行的箱子进行吸取和放置，必须要先将 0、1、2、3 号位置编号的箱子进行吸取和放置后才能够去放置第二行剩下的 4、5、7 三个位置的箱子。

吸取和放置第一行、第二行箱子的策略：**以距当前路径最近为准**（`abs(current_path - pick_path)`）；同种类箱子可重复放置（第二层放置精度已足够）。

**不强制经过中间航点**，路径2，3，4，5的箱子吸取和归还区的节点也没有过渡节点，吸取后，直接到对应的路径的放置区航点即可。

完成8种箱子的吸取和放置规划后，就按src/legged_mission_planner_f_r/config/ui_points_fast_mode.yamlz中的标点进行划线。然后输出五元组序列，png等等，都暂时放到src/legged_mission_planner_f_r/tmpz中

修改方向2：

在标定每个航点时，点击后反馈一个ui，使用者在这个ui进行输入该航点的状态，如果使用者输入状态2/3，那么就还要跳出一个反馈ui,使用者在这个ui进行要吸取或者放置的箱子或者归还区的位置编号，箱子就是0-7，归还区就是0-3，其它状态就是默认输入-1，和修改1中的进行对齐。状态对齐修改方向1中设置的0-3种状态。

修改1中的保存的数据也需要增加，原本的ui_points需要加上航点的状态编号，以及吸取或者放置的位置编号，mission_plan_hardcodedh还是修改1中的数据格式和内容。

这个规划标定的完成确定改成，需要给最后输出的文件命名后缀，加在原本的ui_points以及mission_plan_hardcoded后面，需要使用者自己通过ui界面进行输入后缀名称 2：保存的文件需要放置到对应功能包的源文件中

修改方向1：

现在的路径规划主要就是发送一个完整序列供行为树使用，序列就是(路径，航点，状态，吸取箱子编号/放置对应归还区的箱子编号（如果不是吸取和放置状态就是传-1），是否限制yaw角的bool值)这样的五元组，。

因为现在行为树部分会储存相关的具体航点位置，并且发给机械臂的吸取和放置目标距离是通过雷达获得的baselink和map上标出来的箱子位置或者存放区位置进行偏移量计算，具体的吸取和放置的map位置也是存放到行为树的功能包中的，箱子在map下的位置将以0-7的编号分别进行对应记录，归还区也将以0-3的编号分别对应记录到行为树的功能包中，路径规划只需要在五元组中进行传递，编号含义由前面的状态位决定。

行为树获取到序列后，自行进行具体的解算和导航。

状态规划如下，

| 状态 | 含义       | 使用位置            |
| ---- | ---------- | ------------------- |
| 0    | 智力题识别 | 启动区              |
| 1    | 纯走点     | 过渡航点            |
| 2    | 吸取       | 8个存放区的航点     |
| 3    | 放置       | 4个箱子放置区的航点 |

航点设置，弄一个脚本帮我实现以下要求，帮助我们的路径规划和行为树的对齐，需要对齐src/robot_functionality/legged_mission_bt中的走点要求，实现一个统一输出的yaml文件，这个文件主要就是进行路径规划的航点设置，包括条数，每条路径上的航点。

每个点统一命名为1-1，2-3这种形式，前一个对应路径，后一位对应该路径上的航点

启动脚本后，弹出ui界面，也就是路径规划的界面，使用者进行路径输入，比如我要设置路径1的路径，那么就是在路径输入窗口填上1，然后就是在弹出的图上进行鼠标点击的航点设置，按照1，2，3这样的顺序进行，然后点击完成后，用户自行点击路径航点设置完成的按键ui，每次点击后，在图上显示红色小圈进行反馈。

依次完成所有路径的所有航点设置，然后用户点击路径航点设置完成的按键ui,结束。

输出所有之前点击的路径和航点的在图上的位置保存为ui_points.yaml，这个文件放到src/legged_mission_planner_f_r/config中,用于后续的路径规划的ui点位绘制。同时输出另一个用于行为树读取的带名字的点位信息的yaml文件，命名为mission_plan_hardcoded.yaml，输出到src/robot_functionality/legged_mission_bt/config.

这个yaml文件主要参考这个格式进行输出，src/robot_functionality/legged_mission_bt/config/mission_hardcoded.yaml，不写其中每个点的具体的x,y,yaw坐标（留空），具体坐标后面会用另一个脚本进行获取。

主要是命名和ui_points.yamld对齐就行。

路径规划具体细则暂时不进行修改，保留原本的方式

---

## 航点标定工具（waypoint_editor_fr）

用于在 `场地俯视图.png` 上标定各路径航点的 UI 位置，并与行为树航点命名对齐。每条路径的航点数量**自由**（点完一条路径后点「本路径完成」）。

### 启动

```bash
ros2 launch legged_mission_planner_f_r waypoint_editor.launch.py
# 或
ros2 run legged_mission_planner_f_r waypoint_editor_fr
```

可选参数：

- `--ui-points-out`：ui_points.yaml 输出路径（默认 `config/ui_points.yaml`）
- `--mission-plan-out`：mission_plan_hardcoded.yaml 输出路径（默认写入 `legged_mission_bt/config/`）
- `--load-existing`：从已有 ui_points.yaml 继续编辑（若默认路径存在则自动加载）

### 操作流程

1. 在左侧输入路径号（如 `2`），点「开始」
2. 在场地图上依次左键点击航点，生成 `2-1`、`2-2`、…（红色小圆 + 标签）
3. 点「本路径航点设置完成」锁定该路径，继续下一路径
4. 全部完成后点「全部完成并导出」

### 输出文件

**ui_points.yaml**（`legged_mission_planner_f_r/config/`）— 供后续路径规划 UI 绘制：

```yaml
format_version: 1
planner_variant: front_back
field_image: 场地俯视图.png
coordinate_system: normalized_field_image
paths:
  2:
    - id: "2-1"
      norm_x: 0.537
      norm_y: 0.518
```

**mission_plan_hardcoded.yaml**（`legged_mission_bt/config/`）— 行为树 nav 航点命名模板，id 与 ui_points 一致，**x/y/yaw 留空**：

```yaml
nav:
  "2-1":
    frame_id: "map"
    x: null
    y: null
    yaw: null
```

> **注意**：当前 `WaypointRegistry` 要求 nav 航点 x/y 非空，因此坐标填充完成前**不能**将 `mission_plan_hardcoded.yaml` 作为 `waypoints_file` 直接启动 BT。map 坐标需用后续采集脚本写入。

### 目标架构 vs 当前实现

| 项       | 目标架构（文档顶部）                                        | 当前代码实现                                |
| -------- | ----------------------------------------------------------- | ------------------------------------------- |
| 状态     | 0–3（识别/走点/吸取/放置）                                 | 0–4（含 PICK_FRONT/PICK_BACK/PLACE_FRONT） |
| 规划输出 | 五元组`(path, wp, state, box_or_zone_id, motion_planner)` | `sequence[]` 含 target_box/target_zone    |
| 航点坐标 | BT 包存储，规划只传编号                                     | 本包不导出 pose                             |
| 航点命名 | `P-W`（如 `1-1`）                                       | 航点标定工具已对齐                          |

---

## （一）两阶段路径规划 UI

### 1. UI 标注与列同步

启动 `mission_plan_fr`（`mission_plan.launch.py`）后弹出基于任务赛地图的 UI（`场地俯视图.png`、箱子与归位区图标）。操作手观察场上 8 个箱子，点击 UI 中物资种类后标注各位置。

- 食品/工具/仪器/药品种类编号：0、1、2、3（绿/灰/蓝/红）
- 四个放置区种类顺序可配置，默认 `[0,1,2,3]`，可选 `[3,2,1,0]`
- **列同步**：初始化、重置、切换归位区顺序预设时，每列两个箱子种类自动与对应放置区种类对齐（放置区编号 1 → 该列箱子也为编号 1）。手动单独修改某个箱子后，该箱子保持独立，不再随放置区点击自动覆盖。

**位置编号**（从右往左）：

```
放置区: 3  2  1  0
上排箱: 3  2  1  0
下排箱: 7  6  5  4
```

列映射：`zone_id 0 → box 0,4`；`zone_id 1 → box 1,5`；`zone_id 2 → box 2,6`；`zone_id 3 → box 3,7`。

### 2. 阶段 1：基础路径规划

点击「规划并导出」输出**基础规划**到 `tmp/`（或所选输出目录）：

- `mission_plan.yaml` / `.json` / `.csv` / `.png`
- `mission_quintuple.yaml`
- `base_scenario.yaml`（本次标注快照）
- `plan_source: base`，启动步 `(0,0,1)` 纯走点
- 完成后启用「等待智力题」按钮

### 3. 阶段 2：智力题二次规划（可配置倒计时）

点击「等待智力题」后：

- 侧栏 Spinbox 可调整等待时长（默认 `quiz_wait_timeout_s: 10.0` 秒，范围 1~120）
- 订阅 ROS 话题 `/mission/quiz_type`（`std_msgs/msg/Int32`，值 0~3）
- 倒计时内收到结果 → **自动二次规划**并覆盖导出（`plan_source: quiz`，启动步 `(0,0,0)` 智力题识别）
- 超时未收到 → **不进行智力题规划**，保持基础规划（`quiz_wait_started: true`，`plan_source: base`）

模拟发布：

```bash
ros2 topic pub --once /mission/quiz_type std_msgs/msg/Int32 "{data: 2}"
```

### 4. 启动

```bash
ros2 launch legged_mission_planner_f_r mission_plan.launch.py
```

### 5. 机械条件

机器人向前/向后吸取与放置：**向后吸取、向前放置**。规划输出 `(path, wp, state)` 供行为树分支。

### 6. 路径与航点（每条 3 航点）

保留路径 **2、3、4、5**，每条路径各 **3 个航点**（已删除原路径 1）：

| 路径        | 角色    | wp1                         | wp2                        | wp3                           |
| ----------- | ------- | --------------------------- | -------------------------- | ----------------------------- |
| **2** | 第 0 列 | box4**PICK_FRONT(2)** | box0**PICK_BACK(3)** | zone0**PLACE_FRONT(4)** |
| **3** | 第 1 列 | box5 PICK_BACK              | box1 PICK_BACK             | zone1 PLACE_FRONT             |
| **4** | 第 2 列 | box6 PICK_BACK（末段）      | box2 PICK_BACK（首段）     | zone2 PLACE_FRONT             |
| **5** | 第 3 列 | box7 PICK_BACK              | box3 PICK_BACK             | zone3 PLACE_FRONT             |

**放置路径**：`box_type → zone_id`（由 `zone_types` 决定）→ `place_path = zone_id + 2`。

### 7. 机器人状态

| 状态 | 含义       | 使用位置                       |
| ---- | ---------- | ------------------------------ |
| 0    | 智力题识别 | 启动区（仅 quiz 规划首步）     |
| 1    | 纯走点     | 过渡航点（fast_mode 中不使用） |
| 2    | 向前吸取   | path2 wp1（box4）              |
| 3    | 向后吸取   | path2–5 其余 pick 航点        |
| 4    | 向前放置   | path2–5 wp3                   |

### 8. 四阶段执行流程

1. **path4 首段**：启动区 → path4 wp2 吸取 box2 → 按 box2 种类到对应 path2–5 的 wp3 放置
2. **主任务第一行**：box 0/1/3（最近路径策略）
3. **主任务第二行**：box 4/5/7
4. **path4 末段**：path4 wp1 吸取 box6 → 按 box6 种类到对应 path2–5 的 wp3 放置

### 9. 移动规则

| 场景              | 行为                                            |
| ----------------- | ----------------------------------------------- |
| 跨路径吸取        | 当前 wp → 目标 pick_wp 直接吸取                |
| 跨路径放置        | 当前 wp → 目标 place_wp 直接放置（无过渡走点） |
| path4 box2/6      | 吸取后直达对应归还区放置；五元组仅导出 PLACE    |
| path2–5 同路径内 | pick 后直达 place 航点，不强制经过中间航点      |

### 10. 主策略优先级

主任务组内（path4 固定段与行序硬约束不变）：

1. 智力题种类（`quiz_type` 为 0~3 时）
2. **最近路径优先**（`abs(current_path - pick_path)`）；允许同种类重复放置
3. `box_id` tie-break

### 11. 完整示例

输入：`box_types=[0,1,2,3,0,1,2,3]`，`zone_types=[0,1,2,3]`，`quiz_type=-1`

逻辑序列（mission_plan / png）：

```
(0,0,1) start
(4,2,3) pick box2 (type=2)
(4,3,4) place box2 → zone2
(2,2,3) pick box0
(2,3,4) place box0
... 继续第一行 box1/3，再第二行 box4/5/7
(4,1,3) pick box6 (type=2)
(4,3,4) place box6 → zone2
```

五元组导出（mission_quintuple）：

```
(4,3,4) place box2 → zone2          # 序列首步
(2,2,3) pick box0 ...               # 中间步骤正常导出
...
(4,3,4) place box6 → zone2 (tid=6)  # 序列末步，同 zone 第二次
```

（path4 的 box2/box6 PICK 不导出，由行为树硬编码）

---

## （二）智力题规划说明（两阶段）

仍使用单一 `mission_plan.launch.py`，不再分 `base_plan` / `quiz_plan` 两个 launch。

**阶段 1**：基础规划，`quiz_type` 为 null/-1，`include_quiz_state=false`。

**阶段 2**：等待 `/mission/quiz_type` 后二次规划，组内排序为 `(quiz_rank, distance, box_id)`：

1. 智力题匹配种类优先
2. 最近路径 `abs(current_path - pick_path)`
3. `box_id` tie-break

---

## （三）导出格式

- `format_version: 2`
- `planner_variant: front_back`
- `max_waypoint: 4`
- `switch_mode: fast_mode`
- `sequence[]`: `{path, wp, state, target_box, target_zone, box_type, zone_type, note}`

坐标解析由 `legged_bringup` 负责，本包不导出 pose。

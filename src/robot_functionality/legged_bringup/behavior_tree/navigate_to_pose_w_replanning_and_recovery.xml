<!--
  This Behavior Tree is based on Nav2 default navigate_to_pose_w_replanning_and_recovery.xml.
  Changes from default:
    - Added SmoothPath after ComputePathToPose for path quality
    - SmoothPath output (smoothed_path) used by FollowPath instead of raw path
    - Recovery actions identical to default: ClearCostmap→Spin→Wait→BackUp
  GoalUpdater removed (not needed for waypoint navigation; {goal} passed directly).
  PipelineSequence + RateController 1Hz = standard Nav2 continuous replanning.
-->
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="6" name="NavigateRecovery">
      <PipelineSequence name="NavigateWithReplanning">
        <RateController hz="1.0">
          <RecoveryNode number_of_retries="1" name="ComputePathToPose">
            <Sequence name="ComputeAndSmoothPath">
              <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"/>
              <SmoothPath unsmoothed_path="{path}" smoothed_path="{smoothed_path}"/>
            </Sequence>
            <ClearEntireCostmap name="ClearGlobalCostmap-Context" service_name="global_costmap/clear_entirely_global_costmap"/>
          </RecoveryNode>
        </RateController>
        <RecoveryNode number_of_retries="1" name="FollowPath">
          <FollowPath path="{smoothed_path}" controller_id="FollowPath"/>
          <ClearEntireCostmap name="ClearLocalCostmap-Context" service_name="local_costmap/clear_entirely_local_costmap"/>
        </RecoveryNode>
      </PipelineSequence>
      <ReactiveFallback name="RecoveryFallback">
        <GoalUpdated/>
        <RoundRobin name="RecoveryActions">
          <!-- <BackUp backup_dist="0.3" backup_speed="0.4"/> -->
          <!-- <BackUp backup_dist="0.5" backup_speed="0.4"/> -->
          <!-- <Spin spin_dist="0.523"/> -->
        </RoundRobin>
      </ReactiveFallback>
    </RecoveryNode>
  </BehaviorTree>
</root>
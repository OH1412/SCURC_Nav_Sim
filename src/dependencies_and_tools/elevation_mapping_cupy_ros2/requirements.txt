# ==============================================================================
# 核心依赖：elevation_mapping_cupy_ros2 运行必需（版本与你的环境严格匹配）
# ==============================================================================

# GPU 计算核心（必须匹配 CUDA 12.x，你的版本：12.2.0）
cupy-cuda12x==12.2.0

# 数据处理与数值计算（地形高程/可通行性计算基础）
numpy==1.26.4
scipy==1.15.3
Shapely==1.7.1          # 几何运算（点云裁剪、地形边界判断）
transforms3d==0.4.2     # 3D 坐标变换（与 TF 配合）
fastrlock==0.8.3        # CuPy 依赖的高效锁机制

# 视觉与点云处理（点云解析、图像转换）
opencv-python==4.9.0.80 # 图像预处理（若启用语义融合）
cv-bridge==3.2.1        # ROS 2 图像与 OpenCV 格式转换
laser-geometry==2.4.1   # 激光雷达点云与几何运算（如投影）

# ROS 2 Python 核心依赖（节点通信、参数解析、TF 处理）
rclpy==3.3.17           # ROS 2 Python 客户端库
rcutils==5.1.7          # ROS 2 工具函数（日志、参数管理）
tf2-py==0.25.16         # TF 2 核心 Python 库
tf2-ros-py==0.25.16     # TF 2 ROS 2 接口（坐标变换订阅/发布）
tf2-geometry-msgs==0.25.16 # TF 2 几何消息转换（点云/姿态变换）
grid-map-msgs==2.0.1    # 栅格地图消息类型（与 elevation_map_msgs 配合）

# 配置与序列化（参数文件解析、消息序列化）
PyYAML==6.0.3           # 解析 YAML 配置文件（core_param.yaml）
protobuf==6.33.1        # 消息序列化（ROS 2 消息依赖）

# 可选依赖：若启用 PyTorch 语义融合/深度学习地形分类
torch==2.4.0+cu121      # PyTorch GPU 版本（匹配 CUDA 12.1）
torchaudio==2.4.0+cu121 # 音频处理（若未用可删除）
torchvision==0.19.0+cu121 # 视觉处理（语义分割相关，若未用可删除）

# 可选依赖：其他辅助工具（若未用可删除）
chainer==7.8.1          # 深度学习框架（部分融合算法可能依赖）
scikit-image==0.25.2    # 图像分割/处理（语义地形分析）
matplotlib==3.10.7      # 可视化（若启用日志绘图）

# 修改记录 — 2026-06-30

> 目的：修复段4"假成功"（NavigateToPose瞬间返回成功但机器人未到达目标）+ 障碍物卡住问题。
> **原始值 = git HEAD 已提交版本。**
> 回退方式：参考下面的"回退命令"。

---

## 改动1：行为树 BT XML

**文件**: `src/robot_functionality/legged_bringup/behavior_tree/navigate_to_pose_w_replanning_and_recovery.xml`

### 改动1a：去掉 GoalUpdater，直接传目标

```xml
<!-- 原始 -->
<GoalUpdater input_goal="{goal}" output_goal="{updated_goal}">
    <ComputePathToPose goal="{updated_goal}" path="{path}" planner_id="GridBased"/>
</GoalUpdater>

<!-- 新 -->
<ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"/>
```

### 改动1b：恢复动作取消注释（内容不变）

```xml
<!-- 原始（全部注释） -->
<!-- <BackUp backup_dist="0.3" backup_speed="0.4"/> -->
<!-- <BackUp backup_dist="0.5" backup_speed="0.4"/> -->
<!-- <Spin spin_dist="0.523"/> -->

<!-- 新（仅取消注释，内容不变） -->
<BackUp backup_dist="0.3" backup_speed="0.4"/>
<BackUp backup_dist="0.5" backup_speed="0.4"/>
<Spin spin_dist="0.523"/>
```

### 改动1c：SmoothPath 从 GoalUpdater 外部移入 Sequence 内部

```xml
<!-- 原始：GoalUpdater 只包裹 ComputePathToPose，SmoothPath 在外层 -->
<Sequence name="ComputeAndSmoothPath">
    <GoalUpdater ...>
        <ComputePathToPose .../>
    </GoalUpdater>
    <SmoothPath unsmoothed_path="{path}" smoothed_path="{smoothed_path}"/>
</Sequence>

<!-- 新：去掉 GoalUpdater，两者平级在 Sequence 内 -->
<Sequence name="ComputeAndSmoothPath">
  <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"/>
  <SmoothPath unsmoothed_path="{path}" smoothed_path="{smoothed_path}"/>
</Sequence>
```

### 完整原始XML（git HEAD 版本）

```xml
<!--
  This Behavior Tree replans the global path periodically at 1 Hz and it also has
  recovery actions specific to planning / control as well as general system issues.
  This will be continuous if a kinematically valid planner is selected.
-->
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="6" name="NavigateRecovery">
      <PipelineSequence name="NavigateWithReplanning">
        <RateController hz="1.0">
          <RecoveryNode number_of_retries="1" name="ComputePathToPose">
            <Sequence name="ComputeAndSmoothPath">
                <GoalUpdater input_goal="{goal}" output_goal="{updated_goal}">
                    <ComputePathToPose goal="{updated_goal}" path="{path}" planner_id="GridBased"/>
                </GoalUpdater>
                <SmoothPath unsmoothed_path="{path}" smoothed_path="{smoothed_path}"/>
            </Sequence>

            <ClearEntireCostmap name="ClearGlobalCostmap-Context" service_name="global_costmap/clear_entirely_global_costmap"/>
          </RecoveryNode>
        </RateController>
        <RecoveryNode number_of_retries="1" name="FollowPath">
          <FollowPath path="{smoothed_path}" controller_id="FollowPath"/>
          <ClearEntireCostmap name="ClearLocalCostmap-Context" service_name="local_costmap/clear_entirely_local_costmap"/>
        </RecoveryNode>
      </PipelineSequence>
      <ReactiveFallback name="RecoveryFallback">
        <GoalUpdated/>
        <RoundRobin name="RecoveryActions">
          <!-- <BackUp backup_dist="0.3" backup_speed="0.4"/> -->
          <!-- <BackUp backup_dist="0.5" backup_speed="0.4"/> -->
          <!-- <Spin spin_dist="0.523"/> -->
        </RoundRobin>
      </ReactiveFallback>
    </RecoveryNode>
  </BehaviorTree>
</root>
```

**回退命令**:
```bash
git checkout HEAD -- src/robot_functionality/legged_bringup/behavior_tree/navigate_to_pose_w_replanning_and_recovery.xml
```

> 注意：BT XML 是从 `install/` 软链接到 `src/` 的，修改即时生效，**无需 colcon build**。

---

## 改动2：nav2_params.yaml

**文件**: `src/robot_functionality/legged_bringup/params/nav2_params.yaml`

| # | 参数 | 原始值 (git HEAD) | 新值 | 原因 |
|---|------|-------------------|------|------|
| 2a | `bt_navigator.ros__parameters.default_server_timeout` | `20` | `50` | 给规划服务器响应留更多余量 |
| 2b | `controller_server...ObstacleFootprint.scale` | `50.0` | `20.0` | 降低障碍物惩罚，减少DWB冻结 |
| 2c | `obstacle_scale_controller...normal_scale` | `50.0` | `20.0` | 与 ObstacleFootprint.scale 保持一致 |

**回退命令**:
```bash
git checkout HEAD -- src/robot_functionality/legged_bringup/params/nav2_params.yaml
```

---

## 改动3：navigation.launch.py — 添加 map→odom TF（冗余）

**文件**: `src/robot_functionality/legged_bringup/launch/navigation.launch.py`

新增 380-392 行：`map→odom` 静态 TF 发布者。

| 状态 | 说明 |
|------|------|
| **冗余** | `static_tf_broadcaster.py` + `static_tf_params.yaml` 已经发布了相同的 `map→odom` 恒等变换 |
| 影响 | 无害，可以保留也可以删除 |

**回退命令**:
```bash
git checkout HEAD -- src/robot_functionality/legged_bringup/launch/navigation.launch.py
```

---

## 改动4：obstacle_scale_controller.py — 降低穿越阈值

**文件**: `src/robot_functionality/legged_bringup/nodes/obstacle_scale_controller.py`

| 参数 | 原始值 (git HEAD) | 新值 | 原因 |
|------|-------------------|------|------|
| `FATAL_THRESHOLD` | `253` | `200` | 静态地图障碍物cost=200，253不触发→改为200可触发穿越 |

**回退命令**:
```bash
git checkout HEAD -- src/robot_functionality/legged_bringup/nodes/obstacle_scale_controller.py
```

---

## 改动5：position_based_param_switcher.py — 区域边界调整

**文件**: `src/robot_functionality/legged_bringup/nodes/position_based_param_switcher.py`

| 参数 | 原始值 (git HEAD) | 新值 | 原因 |
|------|-------------------|------|------|
| MIDDLE zone 边界 | `lower=1.0, upper=4.9` | `lower=1.35, upper=4.0` | 收紧中间区（锁yaw、限制vy）范围 |
| EDGE zone hysteresis | 无 | 加了迟滞逻辑 | 防止在边界附近来回切换 |

**回退命令**:
```bash
git checkout HEAD -- src/robot_functionality/legged_bringup/nodes/position_based_param_switcher.py
```

---

## 测试日志汇总

| 时间 | 文件 | BT结构 | 段1-3 /plan | 段4 /plan | 段4结果 |
|------|------|--------|-------------|-----------|---------|
| 01:53 | `...013851.txt` | 原始(PipelineSequence+hz1.0+GoalUpdater) | 29-36条 | 0条 | 0.88s假成功 |
| 02:29 | `...022950.txt` | hz改为10.0 | 29-36条 | 0条 | 0.52s假成功 |
| 02:47 | `...024725.txt` | PipelineSeq→Seq, RateController移除 | 1条 | 0条 | 0.99s假成功 |
| 03:00 | `...030015.txt` | Seq+GoalUpdater | 1条 | 0条 | 10.52s，沿旧路径逆行 |
| **当前** | **待测试** | **PipelineSequence+hz1.0，无GoalUpdater** | ? | ? | ? |
| 03:28 | `...032857.txt` | PipelineSequence+hz1.0+无GoalUpdater+恢复启用(BackUp×2+Spin) | 1条 | 0条 | 10.62s，后退2.5m（恢复动作导致） |
| **待测试** | **待测试** | **恢复注释+段4额外等待3s+action诊断** | ? | ? | ? |

---

## 改动6：注释恢复动作（阻止后退）

**文件**: `src/robot_functionality/legged_bringup/behavior_tree/navigate_to_pose_w_replanning_and_recovery.xml`

**原因**: 恢复动作中的 BackUp×2 (0.3m+0.5m) × 最多6次重试 = 机器人一路后退。段4的假成功触发恢复循环，导致机器人从 x=4.37 退到 x=2.99。

```xml
<!-- 新（恢复动作重新注释） -->
<RoundRobin name="RecoveryActions">
  <!-- <BackUp backup_dist="0.3" backup_speed="0.4"/> -->
  <!-- <BackUp backup_dist="0.5" backup_speed="0.4"/> -->
  <!-- <Spin spin_dist="0.523"/> -->
</RoundRobin>
```

**回退命令**: 取消注释即可。

---

## 改动7：测试脚本增强诊断

**文件**: `src/robot_functionality/legged_bringup/test/path1_nav_test.py`

**新增诊断**:
- 订阅 `/compute_path_to_pose/_action/status` 监控规划器 action 生命周期
- 段4前额外等待 3s（排查 planner_server 状态切换延迟）
- 段间延迟从 0.2s 增加到 0.5s
- 每段开始重置 action 状态历史
- 结果回调中打印本段所有 action 状态事件

**回退命令**:
```bash
git checkout HEAD -- src/robot_functionality/legged_bringup/test/path1_nav_test.py
```

---

## 改动8：关闭重定位 + Fast-LIVO 建图/保存

> 用户不需要重定位功能，且希望关闭 Fast-LIVO 的自动建图/保存来节省计算量。

### 改动8a：新建 Fast-LIVO 精简配置

**文件**: `src/robot_functionality/legged_bringup/params/avia_minimal.yaml`（新文件）

基于 `fast_livo/config/avia.yaml`，修改两项：

| 参数 | 原始值 | 新值 | 原因 |
|------|--------|------|------|
| `pcd_save.pcd_save_en` | `true` | `false` | 关闭 PCD 自动保存（磁盘IO+CPU） |
| `publish.dense_map_en` | `true` | `false` | 关闭稠密地图发布（省CPU/内存） |

### 改动8b：global_relocalization.launch.py 修改

**文件**: `src/robot_functionality/legged_bringup/launch/global_relocalization.launch.py`

| 变更 | 说明 |
|------|------|
| 新增 `enable_relocalization` 参数 | 默认 `false` |
| `transform_publisher` 加 `condition=IfCondition(enable_relocalization)` | 重定位关闭时不启动 |
| `teaser_gicp_node` 加 `condition=IfCondition(enable_relocalization)` | 重定位关闭时不启动（省16线程 GICP） |
| Fast-LIVO 改用 `avia_minimal.yaml` | 从 user params 读精简配置 |
| `static_tf` 改为 TimerAction | 重定位关闭时 3s 启动，开启时 10s 启动（原等待 teaser_gicp 退出） |
| 移除 `RegisterEventHandler`/`OnProcessExit` import | 不再使用 |

### 改动8c：bringup_all_in_one.launch.py 传参

**文件**: `src/robot_functionality/legged_bringup/launch/bringup_all_in_one.launch.py`

向 `global_relocalization.launch.py` 显式传递 `enable_relocalization: 'false'`。

**回退命令**:
```bash
# 8a: 删除新文件
rm src/robot_functionality/legged_bringup/params/avia_minimal.yaml
# 8b-8c: git 回退
git checkout HEAD -- src/robot_functionality/legged_bringup/launch/global_relocalization.launch.py
git checkout HEAD -- src/robot_functionality/legged_bringup/launch/bringup_all_in_one.launch.py
```

**如需临时启用重定位**:
```bash
ros2 launch legged_bringup bringup_in_real.launch.py enable_relocalization:=true
```

---

## 改动9：静态 TF 补充 odom→camera_init

**文件**: `src/robot_functionality/legged_bringup/params/static_tf_params.yaml`

**原因**: 关闭 teaser_gicp 后 `map→camera_init` 不再发布，TF 树断裂。Fast-LIVO 无法发布 `/aft_mapped_in_map`，导致机器人不站起、地图不可用。

新增 `t2: odom→camera_init` 恒等变换，补全 TF 树：`map → odom(identity) → camera_init(identity) → aft_mapped → base_link`

**回退命令**:
```bash
git checkout HEAD -- src/robot_functionality/legged_bringup/params/static_tf_params.yaml
```

---

## 改动10：测试脚本加规划器探针

**文件**: `src/robot_functionality/legged_bringup/test/path1_nav_test.py`

**新增**:
- `ComputPathToPose` ActionClient — 直接调用 planner_server 获取 Result
- `_diagnose_planner()` 方法 — 同步发送 ComputePathToPose goal，返回 path_len、planning_time、error
- 段4 发 NavigateToPose 前自动探针诊断
- 段失败且 0 /plan 时自动追加探针诊断

**回退命令**:
```bash
git checkout HEAD -- src/robot_functionality/legged_bringup/test/path1_nav_test.py
```

---

## 测试日志汇总（更新）

| 时间 | 文件 | BT结构 | 段1-3 /plan | 段4 /plan | 段4结果 |
|------|------|--------|-------------|-----------|---------|
| 01:53 | `...013851.txt` | 原始(PipelineSequence+hz1.0+GoalUpdater) | 29-36条 | 0条 | 0.88s假成功 |
| 02:29 | `...022950.txt` | hz改为10.0 | 29-36条 | 0条 | 0.52s假成功 |
| 02:47 | `...024725.txt` | PipelineSeq→Seq, RateController移除 | 1条 | 0条 | 0.99s假成功 |
| 03:00 | `...030015.txt` | Seq+GoalUpdater | 1条 | 0条 | 10.52s，沿旧路径逆行 |
| 03:28 | `...032857.txt` | PipelineSeq+hz1.0+GoalUpdater+恢复启用 | 1条 | 0条 | 10.62s，后退2.5m |
| 04:04 | `...040401.txt` | PipelineSeq+hz1.0+恢复注释+段4等3s+action诊断 | 6条 | 0条 | 3.62s，2次ABORTED |
| **待测试** | **待测试** | **同上 + 规划器探针** | ? | ? | ? |
